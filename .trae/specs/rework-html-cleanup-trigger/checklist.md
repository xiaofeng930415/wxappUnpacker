- [x] 清理流程在终端运行时可见“triggered/finished”日志
- [x] 清理流程在被参数跳过时可见明确“skip reason”日志
- [x] 主链路未到达回调时，兜底清理判定仍会执行
- [x] `-d` 参数语义正确：跳过删除且保留可追踪日志
- [x] `-o` 参数语义正确：不依赖后处理链路也能完成清理判定
- [x] `.html` 删除覆盖目标目录下嵌套层级文件
- [x] `reverse_future`、`wxappUnpacker`、`shyd_mp`、`ttt` 抽样结果已记录并可复核
